このレビューを投稿しました！

  <div class="reviews">
    <div class="reviews_box_image">
      <%= image_tag @post.image.url, size: '200x200' %>
    </div>
    <div class= "reviews_box_text">
      <div class= "reviews_box_text1">
        <span>タイトル：<%= @post.title %></span>
      </div> 
      <div class= "reviews_box_content">
        <p>コメント<%= @post.content %></p>
      </div>   
    </div> 
  </div>


   <div class="user_edit">
  <% if @post && @post.user_id == current_user.id %>
    <% if @workshop %>
      <%= link_to "他のレビュー見に行く！", users_posts_index_path(current_user.id, @workshop.id), class: "link_text" %>

    <% else %>
      <p>Workshop not found.</p>
    <% end %>

    <!-- Here the edit path should also be corrected -->
    <% if @workshop %>
      <p><%= link_to "編集", users_workshops_edit_path(@workshop, @post), class: "link_text" %></p>
    <% else %>
      <p>Edit not available.</p>
    <% end %>
  <% end %>
</div>

 